exports.bountySystem = () => {
	// Find roles that want to participate (opted in)
	// select someone on the server to become wanted
	// tell them their objective and what they have to do to get points
	// await confirmation to participate as wanted (12 hr timeout)
	// if timeout or decline, look for another user
	// if yes change their role to wanted and notify the server
};
